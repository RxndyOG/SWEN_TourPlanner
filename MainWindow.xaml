<Window x:Class="SWEN_TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SWEN_TourPlanner"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="8*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <local:TitleUserControl Grid.Row="0"/>
        <local:SearchBarUserControl Grid.Row="2"/>
        <local:ItemBarUserControl Grid.Row="1"/>

        <local:TourItemBarUserControl Grid.Row="3"/>


        <Border Grid.Row="4" BorderThickness="0,1,0,0" BorderBrush="Black">
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="1" x:Name="HelloWorld" Content="{Binding Output}"></Label>
                <Button Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Content="Submit" x:Name="HelloWorldBtn" Command="{Binding ExecuteCommand}"></Button>
                <TextBox Grid.Row="0" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            </Grid>
        </Border>
    </Grid>
</Window>
