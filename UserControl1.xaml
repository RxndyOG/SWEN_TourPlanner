<UserControl x:Class="SWEN_TourPlanner.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWEN_TourPlanner"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Grid.Row="4" BorderThickness="0,1,0,0" BorderBrush="Black">
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="1" x:Name="HelloWorld" Content="{Binding Output}"></Label>
                <Button Width="Auto" Height="Auto" Grid.Column="1" Grid.Row="0" Content="Submit" x:Name="HelloWorldBtn" Command="{Binding ExecuteCommand}"></Button>
                <TextBox Grid.Row="0" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
